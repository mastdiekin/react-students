import React from "react";
import classes from "./Fof.sass";

const fof = (props) => {
  return (
    <div className={classes.fof}>
      Ошибка 404. <br /> Страница не найдена
    </div>
  );
};

export default fof;
